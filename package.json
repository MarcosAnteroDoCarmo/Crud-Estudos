{
  "name": "crud-estudos",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node dist/index.js",
    "buid": "yarn tsc --build",
    "dev": "ts-node src/index.ts",
    "setup": "yarn add @types/node typescript && yarn add ts-node -D && yarn tsc --init"
  },
  "dependencies": {
    "@types/node": "^17.0.40",
    "typescript": "^4.7.3"
  },
  "devDependencies": {
    "ts-node": "^10.8.1"
  }
}
